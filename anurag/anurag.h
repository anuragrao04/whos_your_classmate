#include "../definitions.h"

void get_quartiles_for_a_class(tree_node_t* _class); // since class is a reserved keyboard in C, we use _class instead
void insert_into_tree(tree_node_t** class_tree, tree_node_t* student);
